// находим главный контейнер с id 'app'
const app = document.getElementById('app');

// функция для рендеринга интерфейса
function render() {
	app.innerHTML = ''; // очищаем содержимое контейнера
	console.log('render'); // логируем рендеринг


}